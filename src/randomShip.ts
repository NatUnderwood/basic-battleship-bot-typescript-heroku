import { RandomTarget } from './randomTarget'
export class RandomShip {
    constructor(length: number) { }
    public placeShip() {
    var randomTarget = new RandomTarget;
    var firstSquare = randomTarget.findRandomTarget(100)
    
    var startsquare;
    var endSquare;
    }
}